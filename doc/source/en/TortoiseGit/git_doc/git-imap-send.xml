<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect2 SYSTEM "../../../dtd/dblite.dtd">

<sect2 lang="en" id="git-imap-send(1)">
    <title>git-imap-send(1)</title>
<indexterm>
<primary>git-imap-send(1)</primary>
</indexterm>
<simplesect id="git-imap-send(1)__name">
<title>NAME</title>
<simpara>git-imap-send - Send a collection of patches from stdin to an IMAP folder</simpara>
</simplesect>
<simplesect id="git-imap-send(1)__synopsis">
<title>SYNOPSIS</title>
<blockquote>
<literallayout><emphasis>git imap-send</emphasis> [-v] [-q] [--[no-]curl] [(--folder|-f) &lt;folder&gt;]
<emphasis>git imap-send</emphasis> --list</literallayout>
</blockquote>
</simplesect>
<simplesect id="git-imap-send(1)__description">
<title>DESCRIPTION</title>
<simpara>This command uploads a mailbox generated with <emphasis>git format-patch</emphasis>
into an IMAP drafts folder.  This allows patches to be sent as
other email is when using mail clients that cannot read mailbox
files directly. The command also works with any general mailbox
in which emails have the fields <emphasis>From</emphasis>, <emphasis>Date</emphasis>, and <emphasis>Subject</emphasis> in
that order.</simpara>
<simpara>Typical usage is something like:</simpara>
<screen>$ git format-patch --signoff --stdout --attach origin | git imap-send</screen>
</simplesect>
<simplesect id="git-imap-send(1)__options">
<title>OPTIONS</title>
<variablelist>
<varlistentry>
<term>
-v
</term>
<term>
--verbose
</term>
<listitem>
<simpara>
        Be verbose.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
-q
</term>
<term>
--quiet
</term>
<listitem>
<simpara>
        Be quiet.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
-f &lt;folder&gt;
</term>
<term>
--folder=&lt;folder&gt;
</term>
<listitem>
<simpara>
        Specify the folder in which the emails have to saved.
        For example: <emphasis>--folder=[Gmail]/Drafts</emphasis> or <emphasis>-f INBOX/Drafts</emphasis>.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
--curl
</term>
<listitem>
<simpara>
        Use libcurl to communicate with the IMAP server, unless tunneling
        into it.  Ignored if Git was built without the USE_CURL_FOR_IMAP_SEND
        option set.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
--no-curl
</term>
<listitem>
<simpara>
        Talk to the IMAP server using git's own IMAP routines instead of
        using libcurl.  Ignored if Git was built with the NO_OPENSSL option
        set.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
--list
</term>
<listitem>
<simpara>
        Run the IMAP LIST command to output a list of all the folders present.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</simplesect>
<simplesect id="git-imap-send(1)__configuration">
<title>CONFIGURATION</title>
<simpara>To use the tool, <emphasis>imap.folder</emphasis> and either <emphasis>imap.tunnel</emphasis> or <emphasis>imap.host</emphasis> must be set
to appropriate values.</simpara>
<simpara>Everything above this line in this section isn't included from the
<xref linkend="git-config(1)" /> documentation. The content that follows is the
same as what's found there:</simpara>
<variablelist>
<varlistentry>
<term>
imap.folder
</term>
<listitem>
<simpara>
        The folder to drop the mails into, which is typically the Drafts
        folder. For example: <emphasis>INBOX.Drafts</emphasis>, <emphasis>INBOX/Drafts</emphasis> or
        <emphasis>[Gmail]/Drafts</emphasis>. The IMAP folder to interact with MUST be specified;
        the value of this configuration variable is used as the fallback
        default value when the <emphasis>--folder</emphasis> option is not given.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
imap.tunnel
</term>
<listitem>
<simpara>
        Command used to set up a tunnel to the IMAP server through which
        commands will be piped instead of using a direct network connection
        to the server. Required when imap.host is not set.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
imap.host
</term>
<listitem>
<simpara>
        A URL identifying the server. Use an <emphasis>imap://</emphasis> prefix for non-secure
        connections and an <emphasis>imaps://</emphasis> prefix for secure connections.
        Ignored when imap.tunnel is set, but required otherwise.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
imap.user
</term>
<listitem>
<simpara>
        The username to use when logging in to the server.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
imap.pass
</term>
<listitem>
<simpara>
        The password to use when logging in to the server.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
imap.port
</term>
<listitem>
<simpara>
        An integer port number to connect to on the server.
        Defaults to 143 for imap:// hosts and 993 for imaps:// hosts.
        Ignored when imap.tunnel is set.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
imap.sslverify
</term>
<listitem>
<simpara>
        A boolean to enable/disable verification of the server certificate
        used by the SSL/TLS connection. Default is <emphasis>true</emphasis>. Ignored when
        imap.tunnel is set.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
imap.preformattedHTML
</term>
<listitem>
<simpara>
        A boolean to enable/disable the use of html encoding when sending
        a patch.  An html encoded patch will be bracketed with &lt;pre&gt;
        and have a content type of text/html.  Ironically, enabling this
        option causes Thunderbird to send the patch as a plain/text,
        format=fixed email.  Default is <emphasis>false</emphasis>.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
imap.authMethod
</term>
<listitem>
<simpara>
        Specify the authentication method for authenticating with the IMAP server.
        If Git was built with the NO_CURL option, or if your curl version is older
        than 7.34.0, or if you're running git-imap-send with the <emphasis>--no-curl</emphasis>
        option, the only supported methods are <emphasis>PLAIN</emphasis>, <emphasis>CRAM-MD5</emphasis>, <emphasis>OAUTHBEARER</emphasis>
        and <emphasis>XOAUTH2</emphasis>. If this is not set then <emphasis>git imap-send</emphasis> uses the basic IMAP
        plaintext <emphasis>LOGIN</emphasis> command.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</simplesect>
<simplesect id="git-imap-send(1)__getting_a_list_of_available_folders">
<title>GETTING A LIST OF AVAILABLE FOLDERS</title>
<simpara>In order to send an email to a specific folder, you need to know the correct name of
intended folder in your mailbox. The names like "Junk", "Trash" etc. displayed by
various email clients need not be the actual names of the folders stored in the mail
server of your email provider.</simpara>
<simpara>In order to get the correct folder name to be used with <emphasis>git imap-send</emphasis>, you can run
<emphasis>git imap-send --list</emphasis>. This will display a list of valid folder names. An example
of such an output when run on a Gmail account is:</simpara>
<literallayout class="monospaced">* LIST (\HasNoChildren) "/" "INBOX"
* LIST (\HasChildren \Noselect) "/" "[Gmail]"
* LIST (\All \HasNoChildren) "/" "[Gmail]/All Mail"
* LIST (\Drafts \HasNoChildren) "/" "[Gmail]/Drafts"
* LIST (\HasNoChildren \Important) "/" "[Gmail]/Important"
* LIST (\HasNoChildren \Sent) "/" "[Gmail]/Sent Mail"
* LIST (\HasNoChildren \Junk) "/" "[Gmail]/Spam"
* LIST (\Flagged \HasNoChildren) "/" "[Gmail]/Starred"
* LIST (\HasNoChildren \Trash) "/" "[Gmail]/Trash"</literallayout>
<simpara>Here, you can observe that the correct name for the "Junk" folder is <emphasis>[Gmail]/Spam</emphasis>
and for the "Trash" folder is <emphasis>[Gmail]/Trash</emphasis>. Similar logic can be used to determine
other folders as well.</simpara>
</simplesect>
<simplesect id="git-imap-send(1)__examples">
<title>EXAMPLES</title>
<simpara>Using tunnel mode:</simpara>
<literallayout class="monospaced">[imap]
    folder = "INBOX.Drafts"
    tunnel = "ssh -q -C user@example.com /usr/bin/imapd ./Maildir 2&gt; /dev/null"</literallayout>
<simpara>Using direct mode:</simpara>
<literallayout class="monospaced">[imap]
    folder = "INBOX.Drafts"
    host = imap://imap.example.com
    user = bob
    pass = p4ssw0rd</literallayout>
<simpara>Using direct mode with SSL:</simpara>
<literallayout class="monospaced">[imap]
    folder = "INBOX.Drafts"
    host = imaps://imap.example.com
    user = bob
    pass = p4ssw0rd
    port = 123
    ; sslVerify = false</literallayout>
<note><simpara>You may want to use <emphasis>sslVerify=false</emphasis>
while troubleshooting, if you suspect that the reason you are
having trouble connecting is because the certificate you use at
the private server <emphasis>example.com</emphasis> you are trying to set up (or
have set up) may not be verified correctly.</simpara></note>
<simpara>Using Gmail's IMAP interface:</simpara>
<screen>[imap]
    folder = "[Gmail]/Drafts"
    host = imaps://imap.gmail.com
    user = user@gmail.com
    port = 993</screen>
<simpara>Gmail does not allow using your regular password for <emphasis>git imap-send</emphasis>.
If you have multi-factor authentication set up on your Gmail account, you
can generate an app-specific password for use with <emphasis>git imap-send</emphasis>.
Visit <ulink url="https://security.google.com/settings/security/apppasswords">https://security.google.com/settings/security/apppasswords</ulink> to create
it. Alternatively, use OAuth2.0 authentication as described below.</simpara>
<note><simpara>You might need to instead use: <emphasis>folder = "[Google Mail]/Drafts"</emphasis> if you get an error
that the "Folder doesn't exist". You can also run <emphasis>git imap-send --list</emphasis> to get a
list of available folders.</simpara></note>
<note><simpara>If your Gmail account is set to another language than English, the name of the "Drafts"
folder will be localized.</simpara></note>
<simpara>If you want to use OAuth2.0 based authentication, you can specify
<emphasis>OAUTHBEARER</emphasis> or <emphasis>XOAUTH2</emphasis> mechanism in your config. It is more secure
than using app-specific passwords, and also does not enforce the need of
having multi-factor authentication. You will have to use an OAuth2.0
access token in place of your password when using this authentication.</simpara>
<screen>[imap]
    folder = "[Gmail]/Drafts"
    host = imaps://imap.gmail.com
    user = user@gmail.com
    port = 993
    authmethod = OAUTHBEARER</screen>
<simpara>Using Outlook's IMAP interface:</simpara>
<simpara>Unlike Gmail, Outlook only supports OAuth2.0 based authentication. Also, it
supports only <emphasis>XOAUTH2</emphasis> as the mechanism.</simpara>
<screen>[imap]
    folder = "Drafts"
    host = imaps://outlook.office365.com
    user = user@outlook.com
    port = 993
    authmethod = XOAUTH2</screen>
<simpara>Once the commits are ready to be sent, run the following command:</simpara>
<literallayout class="monospaced">$ git format-patch --cover-letter -M --stdout origin/master | git imap-send</literallayout>
<simpara>Just make sure to disable line wrapping in the email client (Gmail's web
interface will wrap lines no matter what, so you need to use a real
IMAP client).</simpara>
<simpara>In case you are using OAuth2.0 authentication, it is easier to use credential
helpers to generate tokens. Credential helpers suggested in
<xref linkend="git-send-email(1)" /> can be used for <emphasis>git imap-send</emphasis> as well.</simpara>
</simplesect>
<simplesect id="git-imap-send(1)__caution">
<title>CAUTION</title>
<simpara>It is still your responsibility to make sure that the email message
sent by your email program meets the standards of your project.
Many projects do not like patches to be attached.  Some mail
agents will transform patches (e.g. wrap lines, send them as
format=flowed) in ways that make them fail.  You will get angry
flames ridiculing you if you don't check this.</simpara>
<simpara>Thunderbird in particular is known to be problematic.  Thunderbird
users may wish to visit this web page for more information:
  <ulink url="https://kb.mozillazine.org/Plain_text_e-mail_-_Thunderbird#Completely_plain_email">https://kb.mozillazine.org/Plain_text_e-mail_-_Thunderbird#Completely_plain_email</ulink></simpara>
</simplesect>
<simplesect id="git-imap-send(1)__see_also">
<title>SEE ALSO</title>
<simpara><xref linkend="git-format-patch(1)" />, <xref linkend="git-send-email(1)" />, mbox(5)</simpara>
</simplesect>
<simplesect id="git-imap-send(1)__git">
<title>GIT</title>
<simpara>Part of the <xref linkend="git(1)" /> suite</simpara>
</simplesect>
</sect2>
