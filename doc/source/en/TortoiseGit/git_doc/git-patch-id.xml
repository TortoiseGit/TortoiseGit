<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect2 SYSTEM "../../../dtd/dblite.dtd">

<sect2 lang="en" id="git-patch-id(1)">
    <title>git-patch-id(1)</title>
<indexterm>
<primary>git-patch-id(1)</primary>
</indexterm>
<simplesect id="git-patch-id(1)__name">
<title>NAME</title>
<simpara>git-patch-id - Compute unique ID for a patch</simpara>
</simplesect>
<simplesect id="git-patch-id(1)__synopsis">
<title>SYNOPSIS</title>
<blockquote>
<literallayout><code>git</code> <code>patch-id</code> [<code>--stable</code> | <code>--unstable</code> | <code>--verbatim</code>]</literallayout>
</blockquote>
</simplesect>
<simplesect id="git-patch-id(1)__description">
<title>DESCRIPTION</title>
<simpara>Read a patch from the standard input and compute the patch ID for it.</simpara>
<simpara>A "patch ID" is nothing but a sum of SHA-1 of the file diffs associated with a
patch, with line numbers ignored.  As such, it's "reasonably stable", but at
the same time also reasonably unique, i.e., two patches that have the same
"patch ID" are almost guaranteed to be the same thing.</simpara>
<simpara>The main usecase for this command is to look for likely duplicate commits.</simpara>
<simpara>When dealing with <emphasis>git diff-tree --patch</emphasis> output, it takes advantage of
the fact that the patch is prefixed with the object name of the
commit, and outputs two 40-byte hexadecimal strings.  The first
string is the patch ID, and the second string is the commit ID.
This can be used to make a mapping from patch ID to commit ID.</simpara>
</simplesect>
<simplesect id="git-patch-id(1)__options">
<title>OPTIONS</title>
<variablelist>
<varlistentry>
<term>
<emphasis>--verbatim</emphasis>
</term>
<listitem>
<simpara>
        Calculate the patch ID of the input as it is given, do not strip
        any whitespace. Implies <emphasis>--stable</emphasis> and forbids <emphasis>--unstable</emphasis>.
</simpara>
<simpara>This is the default if <emphasis>patchid.verbatim</emphasis> is <emphasis>true</emphasis>.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--stable</emphasis>
</term>
<listitem>
<simpara>
        Use a "stable" sum of hashes as the patch ID. With this option:
</simpara>
<itemizedlist>
<listitem>
<simpara>
Reordering file diffs that make up a patch does not affect the ID.
  In particular, two patches produced by comparing the same two trees
  with two different settings for <emphasis>-O&lt;orderfile&gt;</emphasis> result in the same
  patch ID signature, thereby allowing the computed result to be used
  as a key to index some meta-information about the change between
  the two trees.
</simpara>
</listitem>
<listitem>
<simpara>
The result is different from the value produced by Git 1.9 and older
  or produced when an "unstable" hash (see <emphasis>--unstable</emphasis> below) is
  configured - even when used on a diff output taken without any use
  of <emphasis>-O&lt;orderfile&gt;</emphasis>, thereby making existing databases storing such
  "unstable" or historical patch IDs unusable.
</simpara>
</listitem>
<listitem>
<simpara>
All whitespace within the patch is ignored and does not affect the ID.
</simpara>
</listitem>
</itemizedlist>
<simpara>This is the default if <emphasis>patchid.stable</emphasis> is set to <emphasis>true</emphasis>.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--unstable</emphasis>
</term>
<listitem>
<simpara>
        Use an "unstable" hash as the patch ID. With this option,
        the result produced is compatible with the patch ID value produced
        by Git 1.9 and older and whitespace is ignored.  Users with pre-existing
        databases storing patch IDs produced by Git 1.9 and older (who do not deal
        with reordered patches) may want to use this option.
</simpara>
<simpara>This is the default.</simpara>
</listitem>
</varlistentry>
</variablelist>
</simplesect>
<simplesect id="git-patch-id(1)__git">
<title>GIT</title>
<simpara>Part of the <xref linkend="git(1)" /> suite</simpara>
</simplesect>
</sect2>
