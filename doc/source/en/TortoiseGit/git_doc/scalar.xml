<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect2 SYSTEM "../../../dtd/dblite.dtd">

<sect2 lang="en" id="scalar(1)">
    <title>scalar(1)</title>
<indexterm>
<primary>scalar(1)</primary>
</indexterm>
<simplesect id="scalar(1)__name">
<title>NAME</title>
<simpara>scalar - A tool for managing large Git repositories</simpara>
</simplesect>
<simplesect id="scalar(1)__synopsis">
<title>SYNOPSIS</title>
<blockquote>
<literallayout>scalar clone [--single-branch] [--branch &lt;main-branch&gt;] [--full-clone]
        [--[no-]src] [--[no-]tags] [--[no-]maintenance] &lt;url&gt; [&lt;enlistment&gt;]
scalar list
scalar register [--[no-]maintenance] [&lt;enlistment&gt;]
scalar unregister [&lt;enlistment&gt;]
scalar run ( all | config | commit-graph | fetch | loose-objects | pack-files ) [&lt;enlistment&gt;]
scalar reconfigure [--maintenance=(enable|disable|keep)] [ --all | &lt;enlistment&gt; ]
scalar diagnose [&lt;enlistment&gt;]
scalar delete &lt;enlistment&gt;</literallayout>
</blockquote>
</simplesect>
<simplesect id="scalar(1)__description">
<title>DESCRIPTION</title>
<simpara>Scalar is a repository management tool that optimizes Git for use in large
repositories. Scalar improves performance by configuring advanced Git settings,
maintaining repositories in the background, and helping to reduce data sent
across the network.</simpara>
<simpara>An important Scalar concept is the enlistment: this is the top-level directory
of the project. It usually contains the subdirectory <emphasis>src/</emphasis> which is a Git
worktree. This encourages the separation between tracked files (inside <emphasis>src/</emphasis>)
and untracked files, such as build artifacts (outside <emphasis>src/</emphasis>). When registering
an existing Git worktree with Scalar whose name is not <emphasis>src</emphasis>, the enlistment
will be identical to the worktree.</simpara>
<simpara>The <emphasis>scalar</emphasis> command implements various subcommands, and different options
depending on the subcommand. With the exception of <emphasis>clone</emphasis>, <emphasis>list</emphasis> and
<emphasis>reconfigure --all</emphasis>, all subcommands expect to be run in an enlistment.</simpara>
<simpara>The following options can be specified <emphasis>before</emphasis> the subcommand:</simpara>
<variablelist>
<varlistentry>
<term>
-C &lt;directory&gt;
</term>
<listitem>
<simpara>
        Before running the subcommand, change the working directory. This
        option imitates the same option of <xref linkend="git(1)" />.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
-c &lt;key&gt;=&lt;value&gt;
</term>
<listitem>
<simpara>
        For the duration of running the specified subcommand, configure this
        setting. This option imitates the same option of <xref linkend="git(1)" />.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</simplesect>
<simplesect id="scalar(1)__commands">
<title>COMMANDS</title>
<section id="scalar(1)__clone">
<title>Clone</title>
<variablelist>
<varlistentry>
<term>
clone [&lt;options&gt;] &lt;url&gt; [&lt;enlistment&gt;]
</term>
<listitem>
<simpara>
        Clones the specified repository, similar to <xref linkend="git-clone(1)" />. By
        default, only commit and tree objects are cloned. Once finished, the
        worktree is located at <emphasis>&lt;enlistment&gt;/src</emphasis>.
</simpara>
<simpara>The sparse-checkout feature is enabled (except when run with <emphasis>--full-clone</emphasis>)
and the only files present are those in the top-level directory. Use
<emphasis>git sparse-checkout set</emphasis> to expand the set of directories you want to see,
or <emphasis>git sparse-checkout disable</emphasis> to expand to all files (see
<xref linkend="git-sparse-checkout(1)" /> for more details). You can explore the
subdirectories outside your sparse-checkout by using <emphasis>git ls-tree
HEAD[:&lt;directory&gt;]</emphasis>.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
-b &lt;name&gt;
</term>
<term>
--branch &lt;name&gt;
</term>
<listitem>
<simpara>
        Instead of checking out the branch pointed to by the cloned
        repository's HEAD, check out the <emphasis>&lt;name&gt;</emphasis> branch instead.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
--single-branch
</term>
<term>
--no-single-branch
</term>
<listitem>
<simpara>
        Clone only the history leading to the tip of a single branch, either
        specified by the <emphasis>--branch</emphasis> option or the primary branch remote's
        <emphasis>HEAD</emphasis> points at.
</simpara>
<simpara>Further fetches into the resulting repository will only update the
remote-tracking branch for the branch this option was used for the initial
cloning. If the HEAD at the remote did not point at any branch when
<emphasis>--single-branch</emphasis> clone was made, no remote-tracking branch is created.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
--src
</term>
<term>
--no-src
</term>
<listitem>
<simpara>
        By default, <emphasis>scalar clone</emphasis> places the cloned repository within a
        <emphasis>&lt;entlistment&gt;/src</emphasis> directory. Use <emphasis>--no-src</emphasis> to place the cloned
        repository directly in the <emphasis>&lt;enlistment&gt;</emphasis> directory.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
--tags
</term>
<term>
--no-tags
</term>
<listitem>
<simpara>
        By default, <emphasis>scalar clone</emphasis> will fetch the tag objects advertised by
        the remote and future <emphasis>git fetch</emphasis> commands will do the same. Use
        <emphasis>--no-tags</emphasis> to avoid fetching tags in <emphasis>scalar clone</emphasis> and to configure
        the repository to avoid fetching tags in the future. To fetch tags after
        cloning with <emphasis>--no-tags</emphasis>, run <emphasis>git fetch --tags</emphasis>.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
--full-clone
</term>
<term>
--no-full-clone
</term>
<listitem>
<simpara>
        A sparse-checkout is initialized by default. This behavior can be
        turned off via <emphasis>--full-clone</emphasis>.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
--maintenance
</term>
<term>
--no-maintenance
</term>
<listitem>
<simpara>
        By default, <emphasis>scalar clone</emphasis> configures the enlistment to use Git's
        background maintenance feature. Use the <emphasis>--no-maintenance</emphasis> to skip
        this configuration.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</section>
<section id="scalar(1)__list">
<title>List</title>
<variablelist>
<varlistentry>
<term>
list
</term>
<listitem>
<simpara>
        List enlistments that are currently registered by Scalar. This
        subcommand does not need to be run inside an enlistment.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</section>
<section id="scalar(1)__register">
<title>Register</title>
<variablelist>
<varlistentry>
<term>
register [&lt;enlistment&gt;]
</term>
<listitem>
<simpara>
        Adds the enlistment's repository to the list of registered repositories
        and starts background maintenance. If <emphasis>&lt;enlistment&gt;</emphasis> is not provided,
        then the enlistment associated with the current working directory is
        registered.
</simpara>
<simpara>Note: when this subcommand is called in a worktree that is called <emphasis>src/</emphasis>, its
parent directory is considered to be the Scalar enlistment. If the worktree is
<emphasis>not</emphasis> called <emphasis>src/</emphasis>, it itself will be considered to be the Scalar enlistment.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
--maintenance
</term>
<term>
--no-maintenance
</term>
<listitem>
<simpara>
        By default, <emphasis>scalar register</emphasis> configures the enlistment to use Git's
        background maintenance feature. Use the <emphasis>--no-maintenance</emphasis> to skip
        this configuration. This does not disable any maintenance that may
        already be enabled in other ways.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</section>
<section id="scalar(1)__unregister">
<title>Unregister</title>
<variablelist>
<varlistentry>
<term>
unregister [&lt;enlistment&gt;]
</term>
<listitem>
<simpara>
        Remove the specified repository from the list of repositories
        registered with Scalar and stop the scheduled background maintenance.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</section>
<section id="scalar(1)__run">
<title>Run</title>
<variablelist>
<varlistentry>
<term>
scalar run ( all | config | commit-graph | fetch | loose-objects | pack-files ) [&lt;enlistment&gt;]
</term>
<listitem>
<simpara>
        Run the given maintenance task (or all tasks, if <emphasis>all</emphasis> was specified).
        Except for <emphasis>all</emphasis> and <emphasis>config</emphasis>, this subcommand simply hands off to
        <xref linkend="git-maintenance(1)" /> (mapping <emphasis>fetch</emphasis> to <emphasis>prefetch</emphasis> and
        <emphasis>pack-files</emphasis> to <emphasis>incremental-repack</emphasis>).
</simpara>
<simpara>These tasks are run automatically as part of the scheduled maintenance,
as soon as the repository is registered with Scalar. It should therefore
not be necessary to run this subcommand manually.</simpara>
<simpara>The <emphasis>config</emphasis> task is specific to Scalar and configures all those
opinionated default settings that make Git work more efficiently with
large repositories. As this task is run as part of <emphasis>scalar clone</emphasis>
automatically, explicit invocations of this task are rarely needed.</simpara>
</listitem>
</varlistentry>
</variablelist>
</section>
<section id="scalar(1)__reconfigure">
<title>Reconfigure</title>
<simpara>After a Scalar upgrade, or when the configuration of a Scalar enlistment
was somehow corrupted or changed by mistake, this subcommand allows to
reconfigure the enlistment.</simpara>
<variablelist>
<varlistentry>
<term>
--all
</term>
<listitem>
<simpara>
        When <emphasis>--all</emphasis> is specified, reconfigure all enlistments currently
        registered with Scalar by the <emphasis>scalar.repo</emphasis> config key. Use this
        option after each upgrade to get the latest features.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
--maintenance=(enable|disable|keep)
</term>
<listitem>
<simpara>
        By default, Scalar configures the enlistment to use Git's
        background maintenance feature; this is the same as using the
        enable` value for this option. Use the <emphasis>disable</emphasis> value to
        remove each considered enlistment from background maintenance.
        Use `keep to leave the background maintenance configuration
        untouched for these repositories.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</section>
<section id="scalar(1)__diagnose">
<title>Diagnose</title>
<variablelist>
<varlistentry>
<term>
diagnose [&lt;enlistment&gt;]
</term>
<listitem>
<simpara>
    When reporting issues with Scalar, it is often helpful to provide the
    information gathered by this command, including logs and certain
    statistics describing the data shape of the current enlistment.
</simpara>
<simpara>The output of this command is a <emphasis>.zip</emphasis> file that is written into
a directory adjacent to the worktree in the <emphasis>src</emphasis> directory.</simpara>
</listitem>
</varlistentry>
</variablelist>
</section>
<section id="scalar(1)__delete">
<title>Delete</title>
<variablelist>
<varlistentry>
<term>
delete &lt;enlistment&gt;
</term>
<listitem>
<simpara>
        This subcommand lets you delete an existing Scalar enlistment from your
        local file system, unregistering the repository.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</section>
</simplesect>
<simplesect id="scalar(1)__recommended_config_values">
<title>RECOMMENDED CONFIG VALUES</title>
<simpara>As part of both <emphasis>scalar clone</emphasis> and <emphasis>scalar register</emphasis>, certain Git config
values are set to optimize for large repositories or cross-platform support.
These options are updated in new Git versions according to the best known
advice for large repositories, and users can get the latest recommendations
by running <emphasis>scalar reconfigure [--all]</emphasis>.</simpara>
<simpara>This section lists justifications for the config values that are set in the
latest version.</simpara>
<variablelist>
<varlistentry>
<term>
am.keepCR=true
</term>
<listitem>
<simpara>
        This setting is important for cross-platform development across Windows
        and non-Windows platforms and keeping carriage return (<emphasis>\r</emphasis>) characters
        in certain workflows.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
commitGraph.changedPaths=true
</term>
<listitem>
<simpara>
        This setting helps the background maintenance steps that compute the
        serialized commit-graph to also store changed-path Bloom filters. This
        accelerates file history commands and allows users to automatically
        benefit without running a foreground command.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
commitGraph.generationVersion=1
</term>
<listitem>
<simpara>
        While the preferred version is 2 for performance reasons, existing users
        that had version 1 by default will need special care in upgrading to
        version 2. This is likely to change in the future as the upgrade story
        solidifies.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
core.autoCRLF=false
</term>
<listitem>
<simpara>
        This removes the transformation of worktree files to add CRLF line
        endings when only LF line endings exist. This is removed for performance
        reasons. Repositories that use tools that care about CRLF line endings
        should commit the necessary files with those line endings instead.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
core.logAllRefUpdates=true
</term>
<listitem>
<simpara>
        This enables the reflog on all branches. While this is a performance
        cost for large repositories, it is frequently an important data source
        for users to get out of bad situations or to seek support from experts.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
core.safeCRLF=false
</term>
<listitem>
<simpara>
        Similar to <emphasis>core.autoCRLF=false</emphasis>, this disables checks around whether
        the CRLF conversion is reversible. This is a performance improvement,
        but can be dangerous if <emphasis>core.autoCRLF</emphasis> is reenabled by the user.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
credential.https://dev.azure.com.useHttpPath=true
</term>
<listitem>
<simpara>
        This setting enables the <emphasis>credential.useHttpPath</emphasis> feature only for web
        URLs for Azure DevOps. This is important for users interacting with that
        service using multiple organizations and thus multiple credential
        tokens.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
feature.experimental=false
</term>
<listitem>
<simpara>
        This disables the "experimental" optimizations grouped under this
        feature config. The expectation is that all valuable optimizations are
        also set explicitly by Scalar config, and any differences are
        intentional. Notable differences include several bitmap-related config
        options which are disabled for client-focused Scalar repos.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
feature.manyFiles=false
</term>
<listitem>
<simpara>
        This disables the "many files" optimizations grouped under this feature
        config. The expectation is that all valuable optimizations are also set
        explicitly by Scalar config, and any differences are intentional.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
fetch.showForcedUpdates=false
</term>
<listitem>
<simpara>
        This disables the check at the end of <emphasis>git fetch</emphasis> that notifies the user
        if the ref update was a forced update (one where the previous position
        is not reachable from the latest position). This check can be very
        expensive in large repositories, so is disabled and replaced with an
        advice message. Set <emphasis>advice.fetchShowForcedUpdates=false</emphasis> to disable
        this advice message.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
fetch.unpackLimit=1
</term>
<listitem>
<simpara>
        This setting prevents Git from unpacking packfiles into loose objects
        as they are downloaded from the server. The default limit of 100 was
        intended as a way to prevent performance issues from too many packfiles,
        but Scalar uses background maintenance to group packfiles and cover them
        with a multi-pack-index, removing this issue.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
fetch.writeCommitGraph=false
</term>
<listitem>
<simpara>
        This config setting was created to help users automatically update their
        commit-graph files as they perform fetches. However, this takes time
        from foreground fetches and pulls and Scalar uses background maintenance
        for this function instead.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
gc.auto=0
</term>
<listitem>
<simpara>
        This disables automatic garbage collection, since Scalar uses background
        maintenance to keep the repository data in good shape.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
gui.GCWarning=false
</term>
<listitem>
<simpara>
        Since Scalar disables garbage collection by setting <emphasis>gc.auto=0</emphasis>, the
        <emphasis>git-gui</emphasis> tool may start to warn about this setting. Disable this
        warning as Scalar's background maintenance configuration makes the
        warning irrelevant.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
index.skipHash=true
</term>
<listitem>
<simpara>
        Disable computing the hash of the index contents as it is being written.
        This assists with performance, especially for large index files.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
index.threads=true
</term>
<listitem>
<simpara>
        This tells Git to automatically detect how many threads it should use
        when reading the index due to the default value of <emphasis>core.preloadIndex</emphasis>,
        which enables parallel index reads. This explicit setting also enables
        <emphasis>index.recordOffsetTable=true</emphasis> to speed up parallel index reads.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
index.version=4
</term>
<listitem>
<simpara>
        This index version adds compression to the path names, reducing the size
        of the index in a significant way for large repos. This is an important
        performance boost.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
log.excludeDecoration=refs/prefetch/*
</term>
<listitem>
<simpara>
        Since Scalar enables background maintenance with the <emphasis>incremental</emphasis>
        strategy, this setting avoids polluting <emphasis>git log</emphasis> output with refs
        stored by the background prefetch operations.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
merge.renames=true
</term>
<listitem>
<simpara>
        When computing merges in large repos, it is particularly important to
        detect renames to maximize the potential for a result that will validate
        correctly. Users performing merges locally are more likely to be doing
        so because a server-side merge (via pull request or similar) resulted in
        conflicts. While this is the default setting, it is set specifically to
        override a potential change to <emphasis>diff.renames</emphasis> which a user may set for
        performance reasons.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
merge.stat=false
</term>
<listitem>
<simpara>
        This disables a diff output after computing a merge. This improves
        performance of <emphasis>git merge</emphasis> for large repos while reducing noisy output.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
pack.useBitmaps=false
</term>
<listitem>
<simpara>
        This disables the use of <emphasis>.bitmap</emphasis> files attached to packfiles. Bitmap
        files are optimized for server-side use, not client-side use. Scalar
        disables this to avoid some performance issues that can occur if a user
        accidentally creates <emphasis>.bitmap</emphasis> files.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
pack.usePathWalk=true
</term>
<listitem>
<simpara>
        This enables the <emphasis>--path-walk</emphasis> option to <emphasis>git pack-objects</emphasis> by default.
        This can accelerate the computation and compression of packfiles created
        by <emphasis>git push</emphasis> and other repack operations.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
receive.autoGC=false
</term>
<listitem>
<simpara>
        Similar to <emphasis>gc.auto</emphasis>, this setting is disabled in preference of
        background maintenance.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
status.aheadBehind=false
</term>
<listitem>
<simpara>
        This disables the ahead/behind calculation that would normally happen
        during a <emphasis>git status</emphasis> command. This information is frequently ignored by
        users but can be expensive to calculate in large repos that receive
        thousands of commits per day. The calculation is replaced with an advice
        message that can be disabled by disabling the <emphasis>advice.statusAheadBehind</emphasis>
        config.
</simpara>
</listitem>
</varlistentry>
</variablelist>
<simpara>The following settings are different based on which platform is in use:</simpara>
<variablelist>
<varlistentry>
<term>
core.untrackedCache=(true|false)
</term>
<listitem>
<simpara>
        The untracked cache feature is important for performance benefits on
        large repositories, but has demonstrated some bugs on Windows
        filesystems. Thus, this is set for other platforms but disabled on
        Windows.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
http.sslBackend=schannel
</term>
<listitem>
<simpara>
        On Windows, the <emphasis>openssl</emphasis> backend has some issues with certain types of
        remote providers and certificate types. Override the default setting to
        avoid these common problems.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</simplesect>
<simplesect id="scalar(1)__see_also">
<title>SEE ALSO</title>
<simpara><xref linkend="git-clone(1)" />, <xref linkend="git-maintenance(1)" />.</simpara>
</simplesect>
<simplesect id="scalar(1)__git">
<title>GIT</title>
<simpara>Part of the <xref linkend="git(1)" /> suite</simpara>
</simplesect>
</sect2>
