<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sect2 SYSTEM "../../../dtd/dblite.dtd">

<sect2 lang="en" id="git-push(1)">
    <title>git-push(1)</title>
<indexterm>
<primary>git-push(1)</primary>
</indexterm>
<simplesect id="git-push(1)__name">
<title>NAME</title>
<simpara>git-push - Update remote refs along with associated objects</simpara>
</simplesect>
<simplesect id="git-push(1)__synopsis">
<title>SYNOPSIS</title>
<blockquote>
<literallayout><code>git</code> <code>push</code> [<code>--all</code> | <code>--branches</code> | <code>--mirror</code> | <code>--tags</code>] [<code>--follow-tags</code>] [<code>--atomic</code>] [<code>-n</code> | <code>--dry-run</code>] [<code>--receive-pack=</code><emphasis>&lt;git-receive-pack&gt;</emphasis>]
         [<code>--repo=</code><emphasis>&lt;repository&gt;</emphasis>] [<code>-f</code> | <code>--force</code>] [<code>-d</code> | <code>--delete</code>] [<code>--prune</code>] [<code>-q</code> | <code>--quiet</code>] [<code>-v</code> | <code>--verbose</code>]
         [<code>-u</code> | <code>--set-upstream</code>] [<code>-o</code> <emphasis>&lt;string&gt;</emphasis> | <code>--push-option=</code><emphasis>&lt;string&gt;</emphasis>]
         [<code>--</code>[<code>no-</code>]<code>signed</code> | <code>--signed=</code>(<code>true</code>|<code>false</code>|<code>if-asked</code>)]
         [<code>--force-with-lease</code>[<code>=</code><emphasis>&lt;refname&gt;</emphasis>[<code>:</code><emphasis>&lt;expect&gt;</emphasis>]] [<code>--force-if-includes</code>]]
         [<code>--no-verify</code>] [<emphasis>&lt;repository&gt;</emphasis> [<emphasis>&lt;refspec&gt;</emphasis><code>&#8230;</code>]]</literallayout>
</blockquote>
</simplesect>
<simplesect id="git-push(1)__description">
<title>DESCRIPTION</title>
<simpara>Updates one or more branches, tags, or other references in a remote
repository from your local repository, and sends all necessary data
that isn't already on the remote.</simpara>
<simpara>The simplest way to push is <emphasis>git push &lt;remote&gt; &lt;branch&gt;</emphasis>.
<emphasis>git push origin main</emphasis> will push the local <emphasis>main</emphasis> branch to the <emphasis>main</emphasis>
branch on the remote named <emphasis>origin</emphasis>.</simpara>
<simpara>The <emphasis>&lt;repository&gt;</emphasis> argument defaults to the upstream for the current branch,
or <emphasis>origin</emphasis> if there's no configured upstream.</simpara>
<simpara>To decide which branches, tags, or other refs to push, Git uses
(in order of precedence):</simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>
The <emphasis>&lt;refspec&gt;</emphasis> argument(s) (for example <emphasis>main</emphasis> in <emphasis>git push origin main</emphasis>)
   or the <emphasis>--all</emphasis>, <emphasis>--mirror</emphasis>, or <emphasis>--tags</emphasis> options
</simpara>
</listitem>
<listitem>
<simpara>
The <emphasis>remote.&lt;name&gt;.push</emphasis> configuration for the repository being pushed to
</simpara>
</listitem>
<listitem>
<simpara>
The <emphasis>push.default</emphasis> configuration. The default is <emphasis>push.default=simple</emphasis>,
   which will push to a branch with the same name as the current branch.
   See the <link linkend="git-push(1)_CONFIGURATION">CONFIGURATION</link> section below for more on <emphasis>push.default</emphasis>.
</simpara>
</listitem>
</orderedlist>
<simpara><emphasis>git push</emphasis> may fail if you haven't set an upstream for the current branch,
depending on what <emphasis>push.default</emphasis> is set to.
See the <link linkend="git-push(1)_UPSTREAM-BRANCHES">UPSTREAM BRANCHES</link> section below for more
on how to set and use upstreams.</simpara>
<simpara>You can make interesting things happen to a repository
every time you push into it, by setting up <emphasis>hooks</emphasis> there.  See
documentation for <xref linkend="git-receive-pack(1)" />.</simpara>
</simplesect>
<simplesect id="git-push(1)_OPTIONS">
<title>OPTIONS</title>
<variablelist>
<varlistentry>
<term>
<emphasis>&lt;repository&gt;</emphasis>
</term>
<listitem>
<simpara>
        The "remote" repository that is the destination of a push
        operation.  This parameter can be either a URL
        (see the section <link linkend="git-push(1)_URLS">GIT URLS</link> below) or the name
        of a remote (see the section <link linkend="git-push(1)_REMOTES">REMOTES</link> below).
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>&lt;refspec&gt;...</emphasis>
</term>
<listitem>
<simpara>
        Specify what destination ref to update with what source object.
</simpara>
<simpara>The format for a refspec is <emphasis>[+]&lt;src&gt;[:&lt;dst&gt;]</emphasis>, for example <emphasis>main</emphasis>,
<emphasis>main:other</emphasis>, or <emphasis>HEAD^:refs/heads/main</emphasis>.</simpara>
<simpara>The <emphasis>&lt;src&gt;</emphasis> is often the name of the local branch to push, but it can be
any arbitrary "SHA-1 expression" (see <xref linkend="gitrevisions(7)" />).</simpara>
<simpara>The <emphasis>&lt;dst&gt;</emphasis> determines what ref to update on the remote side. It must be the
name of a branch, tag, or other ref, not an arbitrary expression.</simpara>
<simpara>The <emphasis>+</emphasis> is optional and does the same thing as <emphasis>--force</emphasis>.</simpara>
<simpara>You can write a refspec using the fully expanded form (for
example <emphasis>refs/heads/main:refs/heads/main</emphasis>) which specifies the exact source
and destination, or with a shorter form (for example <emphasis>main</emphasis> or
<emphasis>main:other</emphasis>). Here are the rules for how refspecs are expanded,
as well as various other special refspec forms:</simpara>
<itemizedlist>
<listitem>
<simpara>
<emphasis>&lt;src&gt;</emphasis> without a <emphasis>:&lt;dst&gt;</emphasis> means to update the same ref as the
    <emphasis>&lt;src&gt;</emphasis>, unless the <emphasis>remote.&lt;repository&gt;.push</emphasis> configuration specifies a
    different <emphasis>&lt;dst&gt;</emphasis>. For example, if <emphasis>main</emphasis> is a branch, then the refspec
    <emphasis>main</emphasis> expands to <emphasis>main:refs/heads/main</emphasis>.
</simpara>
</listitem>
<listitem>
<simpara>
If <emphasis>&lt;dst&gt;</emphasis> unambiguously refers to a ref on the &lt;repository&gt; remote,
    then expand it to that ref. For example, if <emphasis>v1.0</emphasis> is a tag on the
    remote, then <emphasis>HEAD:v1.0</emphasis> expands to <emphasis>HEAD:refs/tags/v1.0</emphasis>.
</simpara>
</listitem>
<listitem>
<simpara>
If <emphasis>&lt;src&gt;</emphasis> resolves to a ref starting with <emphasis>refs/heads/</emphasis> or <emphasis>refs/tags/</emphasis>,
    then prepend that to &lt;dst&gt;. For example, if <emphasis>main</emphasis> is a branch, then
    <emphasis>main:other</emphasis> expands to <emphasis>main:refs/heads/other</emphasis>
</simpara>
</listitem>
<listitem>
<simpara>
The special refspec <emphasis>:</emphasis> (or <emphasis>+:</emphasis> to allow non-fast-forward updates)
    directs Git to push "matching" branches: for every branch that exists on
    the local side, the remote side is updated if a branch of the same name
    already exists on the remote side.
</simpara>
</listitem>
<listitem>
<simpara>
<emphasis>&lt;src&gt;</emphasis> may contain a <emphasis>*</emphasis> to indicate a simple pattern match.
    This works like a glob that matches any ref matching the pattern.
    There must be only one <emphasis>*</emphasis> in both the <emphasis>&lt;src&gt;</emphasis> and <emphasis>&lt;dst&gt;</emphasis>.
    It will map refs to the destination by replacing the * with the
    contents matched from the source. For example, <emphasis>refs/heads/*:refs/heads/*</emphasis>
    will push all branches.
</simpara>
</listitem>
<listitem>
<simpara>
A refspec starting with <emphasis>^</emphasis> is a negative refspec.
    This specifies refs to exclude. A ref will be considered to
    match if it matches at least one positive refspec, and does not
    match any negative refspec. Negative refspecs can be pattern refspecs.
    They must only contain a <emphasis>&lt;src&gt;</emphasis>.
    Fully spelled out hex object names are also not supported.
    For example, <emphasis>git push origin 'refs/heads/*' '^refs/heads/dev-*'</emphasis>
    will push all branches except for those starting with <emphasis>dev-</emphasis>
</simpara>
</listitem>
<listitem>
<simpara>
If <emphasis>&lt;src&gt;</emphasis> is empty, it deletes the <emphasis>&lt;dst&gt;</emphasis> ref from the remote
    repository. For example, <emphasis>git push origin :dev</emphasis> will
    delete the <emphasis>dev</emphasis> branch.
</simpara>
</listitem>
<listitem>
<simpara>
<emphasis>tag &lt;tag&gt;</emphasis> expands to <emphasis>refs/tags/&lt;tag&gt;:refs/tags/&lt;tag&gt;</emphasis>.
        This is technically a special syntax for <emphasis>git push</emphasis> and not a refspec,
        since in <emphasis>git push origin tag v1.0</emphasis> the arguments <emphasis>tag</emphasis> and <emphasis>v1.0</emphasis>
        are separate.
</simpara>
</listitem>
<listitem>
<simpara>
If the refspec can't be expanded unambiguously, error out
    with an error indicating what was tried, and depending
    on the <emphasis>advice.pushUnqualifiedRefname</emphasis> configuration (see
    <xref linkend="git-config(1)" />) suggest what refs/ namespace you may have
    wanted to push to.
</simpara>
</listitem>
</itemizedlist>
</listitem>
</varlistentry>
</variablelist>
<simpara>Not all updates are allowed: see PUSH RULES below for the details.</simpara>
<variablelist>
<varlistentry>
<term>
<emphasis>--all</emphasis>
</term>
<term>
<emphasis>--branches</emphasis>
</term>
<listitem>
<simpara>
        Push all branches (i.e. refs under <emphasis>refs/heads/</emphasis>); cannot be
        used with other &lt;refspec&gt;.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--prune</emphasis>
</term>
<listitem>
<simpara>
        Remove remote branches that don't have a local counterpart. For example
        a remote branch <emphasis>tmp</emphasis> will be removed if a local branch with the same
        name doesn't exist any more. This also respects refspecs, e.g.
        <emphasis>git push --prune remote refs/heads/*:refs/tmp/*</emphasis> would
        make sure that remote <emphasis>refs/tmp/foo</emphasis> will be removed if <emphasis>refs/heads/foo</emphasis>
        doesn't exist.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--mirror</emphasis>
</term>
<listitem>
<simpara>
        Instead of naming each ref to push, specifies that all
        refs under <emphasis>refs/</emphasis> (which includes but is not
        limited to <emphasis>refs/heads/</emphasis>, <emphasis>refs/remotes/</emphasis>, and <emphasis>refs/tags/</emphasis>)
        be mirrored to the remote repository.  Newly created local
        refs will be pushed to the remote end, locally updated refs
        will be force updated on the remote end, and deleted refs
        will be removed from the remote end.  This is the default
        if the configuration option <emphasis>remote.&lt;remote&gt;.mirror</emphasis> is
        set.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>-n</emphasis>
</term>
<term>
<emphasis>--dry-run</emphasis>
</term>
<listitem>
<simpara>
        Do everything except actually send the updates.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--porcelain</emphasis>
</term>
<listitem>
<simpara>
        Produce machine-readable output.  The output status line for each ref
        will be tab-separated and sent to stdout instead of stderr.  The full
        symbolic names of the refs will be given.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>-d</emphasis>
</term>
<term>
<emphasis>--delete</emphasis>
</term>
<listitem>
<simpara>
        All listed refs are deleted from the remote repository. This is
        the same as prefixing all refs with a colon.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--tags</emphasis>
</term>
<listitem>
<simpara>
        All refs under <emphasis>refs/tags</emphasis> are pushed, in
        addition to refspecs explicitly listed on the command
        line.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--follow-tags</emphasis>
</term>
<listitem>
<simpara>
        Push all the refs that would be pushed without this option,
        and also push annotated tags in <emphasis>refs/tags</emphasis> that are missing
        from the remote but are pointing at commit-ish that are
        reachable from the refs being pushed.  This can also be specified
        with configuration variable <emphasis>push.followTags</emphasis>.  For more
        information, see <emphasis>push.followTags</emphasis> in <xref linkend="git-config(1)" />.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--signed</emphasis>
</term>
<term>
<emphasis>--no-signed</emphasis>
</term>
<term>
<emphasis>--signed=(true|false|if-asked)</emphasis>
</term>
<listitem>
<simpara>
        GPG-sign the push request to update refs on the receiving
        side, to allow it to be checked by the hooks and/or be
        logged. Possible values are:
</simpara>
<variablelist>
<varlistentry>
<term>
<emphasis>false</emphasis>
</term>
<term>
<emphasis>--no-signed</emphasis>
</term>
<listitem>
<simpara>
no signing will be attempted.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>true</emphasis>
</term>
<term>
<emphasis>--signed</emphasis>
</term>
<listitem>
<simpara>
the push will fail if the server does not support signed pushes.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>if-asked</emphasis>
</term>
<listitem>
<simpara>
sign if and only if the server supports signed pushes.  The push
will also fail if the actual call to <emphasis>gpg --sign</emphasis> fails.  See
<xref linkend="git-receive-pack(1)" /> for the details on the receiving end.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--atomic</emphasis>
</term>
<term>
<emphasis>--no-atomic</emphasis>
</term>
<listitem>
<simpara>
        Use an atomic transaction on the remote side if available.
        Either all refs are updated, or on error, no refs are updated.
        If the server does not support atomic pushes the push will fail.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>-o &lt;option&gt;</emphasis>
</term>
<term>
<emphasis>--push-option=&lt;option&gt;</emphasis>
</term>
<listitem>
<simpara>
        Transmit the given string to the server, which passes them to
        the pre-receive as well as the post-receive hook. The given string
        must not contain a <emphasis>NUL</emphasis> or <emphasis>LF</emphasis> character.
        When multiple <emphasis>--push-option=&lt;option&gt;</emphasis> are given, they are
        all sent to the other side in the order listed on the
        command line.
        When no <emphasis>--push-option=&lt;option&gt;</emphasis> is given from the command
        line, the values of configuration variable <emphasis>push.pushOption</emphasis>
        are used instead.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--receive-pack=&lt;git-receive-pack&gt;</emphasis>
</term>
<term>
<emphasis>--exec=&lt;git-receive-pack&gt;</emphasis>
</term>
<listitem>
<simpara>
        Path to the <emphasis>git-receive-pack</emphasis> program on the remote
        end.  Sometimes useful when pushing to a remote
        repository over ssh, and you do not have the program in
        a directory on the default <emphasis>$PATH</emphasis>.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--force-with-lease</emphasis>
</term>
<term>
<emphasis>--no-force-with-lease</emphasis>
</term>
<term>
<emphasis>--force-with-lease=&lt;refname&gt;</emphasis>
</term>
<term>
<emphasis>--force-with-lease=&lt;refname&gt;:&lt;expect&gt;</emphasis>
</term>
<listitem>
<simpara>
        Usually, <emphasis>git push</emphasis> refuses to update a remote ref that is
        not an ancestor of the local ref used to overwrite it.
</simpara>
<simpara>This option overrides this restriction if the current value of the
remote ref is the expected value.  <emphasis>git push</emphasis> fails otherwise.</simpara>
<simpara>Imagine that you have to rebase what you have already published.
You will have to bypass the "must fast-forward" rule in order to
replace the history you originally published with the rebased history.
If somebody else built on top of your original history while you are
rebasing, the tip of the branch at the remote may advance with their
commit, and blindly pushing with <emphasis>--force</emphasis> will lose their work.</simpara>
<simpara>This option allows you to say that you expect the history you are
updating is what you rebased and want to replace. If the remote ref
still points at the commit you specified, you can be sure that no
other people did anything to the ref. It is like taking a "lease" on
the ref without explicitly locking it, and the remote ref is updated
only if the "lease" is still valid.</simpara>
<simpara><emphasis>--force-with-lease</emphasis> alone, without specifying the details, will protect
all remote refs that are going to be updated by requiring their
current value to be the same as the remote-tracking branch we have
for them.</simpara>
<simpara><emphasis>--force-with-lease=&lt;refname&gt;</emphasis>, without specifying the expected value, will
protect <emphasis>&lt;refname&gt;</emphasis> (alone), if it is going to be updated, by
requiring its current value to be the same as the remote-tracking
branch we have for it.</simpara>
<simpara><emphasis>--force-with-lease=&lt;refname&gt;:&lt;expect&gt;</emphasis> will protect <emphasis>&lt;refname&gt;</emphasis> (alone),
if it is going to be updated, by requiring its current value to be
the same as the specified value <emphasis>&lt;expect&gt;</emphasis> (which is allowed to be
different from the remote-tracking branch we have for the refname,
or we do not even have to have such a remote-tracking branch when
this form is used).  If <emphasis>&lt;expect&gt;</emphasis> is the empty string, then the named ref
must not already exist.</simpara>
<simpara>Note that all forms other than <emphasis>--force-with-lease=&lt;refname&gt;:&lt;expect&gt;</emphasis>
that specifies the expected current value of the ref explicitly are
still experimental and their semantics may change as we gain experience
with this feature.</simpara>
<simpara><emphasis>--no-force-with-lease</emphasis> will cancel all the previous <emphasis>--force-with-lease</emphasis> on the
command line.</simpara>
<simpara>A general note on safety: supplying this option without an expected
value, i.e. as <emphasis>--force-with-lease</emphasis> or <emphasis>--force-with-lease=&lt;refname&gt;</emphasis>
interacts very badly with anything that implicitly runs <emphasis>git fetch</emphasis> on
the remote to be pushed to in the background, e.g. <emphasis>git fetch origin</emphasis>
on your repository in a cronjob.</simpara>
<simpara>The protection it offers over <emphasis>--force</emphasis> is ensuring that subsequent
changes your work wasn't based on aren't clobbered, but this is
trivially defeated if some background process is updating refs in the
background. We don't have anything except the remote tracking info to
go by as a heuristic for refs you're expected to have seen &amp; are
willing to clobber.</simpara>
<simpara>If your editor or some other system is running <emphasis>git fetch</emphasis> in the
background for you a way to mitigate this is to simply set up another
remote:</simpara>
<screen>git remote add origin-push $(git config remote.origin.url)
git fetch origin-push</screen>
<simpara>Now when the background process runs <emphasis>git fetch origin</emphasis> the references
on <emphasis>origin-push</emphasis> won't be updated, and thus commands like:</simpara>
<screen>git push --force-with-lease origin-push</screen>
<simpara>Will fail unless you manually run <emphasis>git fetch origin-push</emphasis>. This method
is of course entirely defeated by something that runs <emphasis>git fetch
--all</emphasis>, in that case you'd need to either disable it or do something
more tedious like:</simpara>
<screen>git fetch              # update 'master' from remote
git tag base master    # mark our base point
git rebase -i master   # rewrite some commits
git push --force-with-lease=master:base master:master</screen>
<simpara>I.e. create a <emphasis>base</emphasis> tag for versions of the upstream code that you've
seen and are willing to overwrite, then rewrite history, and finally
force push changes to <emphasis>master</emphasis> if the remote version is still at
<emphasis>base</emphasis>, regardless of what your local <emphasis>remotes/origin/master</emphasis> has been
updated to in the background.</simpara>
<simpara>Alternatively, specifying <emphasis>--force-if-includes</emphasis> as an ancillary option
along with <emphasis>--force-with-lease[=&lt;refname&gt;]</emphasis> (i.e., without saying what
exact commit the ref on the remote side must be pointing at, or which
refs on the remote side are being protected) at the time of "push" will
verify if updates from the remote-tracking refs that may have been
implicitly updated in the background are integrated locally before
allowing a forced update.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>-f</emphasis>
</term>
<term>
<emphasis>--force</emphasis>
</term>
<listitem>
<simpara>
        Usually, <emphasis>git push</emphasis> will refuse to update a branch that is not an
        ancestor of the commit being pushed.
</simpara>
<simpara>This flag disables that check, the other safety checks in PUSH RULES
below, and the checks in <emphasis>--force-with-lease</emphasis>. It can cause the remote
repository to lose commits; use it with care.</simpara>
<simpara>Note that <emphasis>--force</emphasis> applies to all the refs that are pushed, hence
using it with <emphasis>push.default</emphasis> set to <emphasis>matching</emphasis> or with multiple push
destinations configured with <emphasis>remote.&lt;name&gt;.push</emphasis> may overwrite refs
other than the current branch (including local refs that are
strictly behind their remote counterpart).  To force a push to only
one branch, use a <emphasis>+</emphasis> in front of the refspec to push (e.g <emphasis>git push
origin +master</emphasis> to force a push to the <emphasis>master</emphasis> branch). See the
<emphasis>&lt;refspec&gt;...</emphasis> section above for details.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--force-if-includes</emphasis>
</term>
<term>
<emphasis>--no-force-if-includes</emphasis>
</term>
<listitem>
<simpara>
        Force an update only if the tip of the remote-tracking ref
        has been integrated locally.
</simpara>
<simpara>This option enables a check that verifies if the tip of the
remote-tracking ref is reachable from one of the "reflog" entries of
the local branch based in it for a rewrite. The check ensures that any
updates from the remote have been incorporated locally by rejecting the
forced update if that is not the case.</simpara>
<simpara>If the option is passed without specifying <emphasis>--force-with-lease</emphasis>, or
specified along with <emphasis>--force-with-lease=&lt;refname&gt;:&lt;expect&gt;</emphasis>, it is
a "no-op".</simpara>
<simpara>Specifying <emphasis>--no-force-if-includes</emphasis> disables this behavior.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--repo=&lt;repository&gt;</emphasis>
</term>
<listitem>
<simpara>
        This option is equivalent to the <emphasis>&lt;repository&gt;</emphasis> argument. If both
        are specified, the command-line argument takes precedence.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>-u</emphasis>
</term>
<term>
<emphasis>--set-upstream</emphasis>
</term>
<listitem>
<simpara>
        For every branch that is up to date or successfully pushed, add
        upstream (tracking) reference, used by argument-less
        <xref linkend="git-pull(1)" /> and other commands. For more information,
        see <emphasis>branch.&lt;name&gt;.merge</emphasis> in <xref linkend="git-config(1)" />.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--thin</emphasis>
</term>
<term>
<emphasis>--no-thin</emphasis>
</term>
<listitem>
<simpara>
        These options are passed to <xref linkend="git-send-pack(1)" />. A thin transfer
        significantly reduces the amount of sent data when the sender and
        receiver share many of the same objects in common. The default is
        <emphasis>--thin</emphasis>.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>-q</emphasis>
</term>
<term>
<emphasis>--quiet</emphasis>
</term>
<listitem>
<simpara>
        Suppress all output, including the listing of updated refs,
        unless an error occurs. Progress is not reported to the standard
        error stream.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>-v</emphasis>
</term>
<term>
<emphasis>--verbose</emphasis>
</term>
<listitem>
<simpara>
        Run verbosely.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--progress</emphasis>
</term>
<listitem>
<simpara>
        Progress status is reported on the standard error stream
        by default when it is attached to a terminal, unless <emphasis>-q</emphasis>
        is specified. This flag forces progress status even if the
        standard error stream is not directed to a terminal.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--no-recurse-submodules</emphasis>
</term>
<term>
<emphasis>--recurse-submodules=(check|on-demand|only|no)</emphasis>
</term>
<listitem>
<simpara>
        May be used to make sure all submodule commits used by the
        revisions to be pushed are available on a remote-tracking branch.
        Possible values are:
</simpara>
<variablelist>
<varlistentry>
<term>
<emphasis>check</emphasis>
</term>
<listitem>
<simpara>
        Git will verify that all submodule commits that
        changed in the revisions to be pushed are available on at least one
        remote of the submodule. If any commits are missing the push will
        be aborted and exit with non-zero status.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>on-demand</emphasis>
</term>
<listitem>
<simpara>
        all submodules that changed in the revisions to be pushed will be
        pushed. If <emphasis>on-demand</emphasis> was not able to push all necessary revisions it will
        also be aborted and exit with non-zero status.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>only</emphasis>
</term>
<listitem>
<simpara>
        all submodules will be pushed while the superproject is left
        unpushed.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>no</emphasis>
</term>
<listitem>
<simpara>
        override the <emphasis>push.recurseSubmodules</emphasis> configuration variable when no
        submodule recursion is required. Similar to using <emphasis>--no-recurse-submodules</emphasis>.
</simpara>
</listitem>
</varlistentry>
</variablelist>
<simpara>When using <emphasis>on-demand</emphasis> or <emphasis>only</emphasis>, if a submodule has a
<emphasis>push.recurseSubmodules=(on-demand|only)</emphasis> or <emphasis>submodule.recurse</emphasis> configuration,
further recursion will occur. In this case, <emphasis>only</emphasis> is treated as <emphasis>on-demand</emphasis>.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>--verify</emphasis>
</term>
<term>
<emphasis>--no-verify</emphasis>
</term>
<listitem>
<simpara>
        Toggle the pre-push hook (see <xref linkend="githooks(5)" />).  The
        default is <emphasis>--verify</emphasis>, giving the hook a chance to prevent the
        push.  With <emphasis>--no-verify</emphasis>, the hook is bypassed completely.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>-4</emphasis>
</term>
<term>
<emphasis>--ipv4</emphasis>
</term>
<listitem>
<simpara>
        Use IPv4 addresses only, ignoring IPv6 addresses.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>-6</emphasis>
</term>
<term>
<emphasis>--ipv6</emphasis>
</term>
<listitem>
<simpara>
        Use IPv6 addresses only, ignoring IPv4 addresses.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</simplesect>
<simplesect id="git-push(1)__git_urls_anchor_id_git_push_1__urls_xreflabel_urls">
<title>GIT URLS<anchor id="git-push(1)_URLS" xreflabel="[URLS]"/></title>
<simpara>In general, URLs contain information about the transport protocol, the
address of the remote server, and the path to the repository.
Depending on the transport protocol, some of this information may be
absent.</simpara>
<simpara>Git supports ssh, git, http, and https protocols (in addition, ftp
and ftps can be used for fetching, but this is inefficient and
deprecated; do not use them).</simpara>
<simpara>The native transport (i.e. <emphasis>git://</emphasis> URL) does no authentication and
should be used with caution on unsecured networks.</simpara>
<simpara>The following syntaxes may be used with them:</simpara>
<itemizedlist>
<listitem>
<simpara>
<emphasis>ssh://[&lt;user&gt;@]&lt;host&gt;[:&lt;port&gt;]/&lt;path-to-git-repo&gt;</emphasis>
</simpara>
</listitem>
<listitem>
<simpara>
<emphasis>git://&lt;host&gt;[:&lt;port&gt;]/&lt;path-to-git-repo&gt;</emphasis>
</simpara>
</listitem>
<listitem>
<simpara>
<emphasis>http[s]://&lt;host&gt;[:&lt;port&gt;]/&lt;path-to-git-repo&gt;</emphasis>
</simpara>
</listitem>
<listitem>
<simpara>
<emphasis>ftp[s]://&lt;host&gt;[:&lt;port&gt;]/&lt;path-to-git-repo&gt;</emphasis>
</simpara>
</listitem>
</itemizedlist>
<simpara>An alternative scp-like syntax may also be used with the ssh protocol:</simpara>
<itemizedlist>
<listitem>
<simpara>
<emphasis>[&lt;user&gt;@]&lt;host&gt;:/&lt;path-to-git-repo&gt;</emphasis>
</simpara>
</listitem>
</itemizedlist>
<simpara>This syntax is only recognized if there are no slashes before the
first colon. This helps differentiate a local path that contains a
colon. For example the local path <emphasis>foo:bar</emphasis> could be specified as an
absolute path or <emphasis>./foo:bar</emphasis> to avoid being misinterpreted as an ssh
url.</simpara>
<simpara>The ssh and git protocols additionally support <emphasis>~&lt;username&gt;</emphasis> expansion:</simpara>
<itemizedlist>
<listitem>
<simpara>
<emphasis>ssh://[&lt;user&gt;@]&lt;host&gt;[:&lt;port&gt;]/~&lt;user&gt;/&lt;path-to-git-repo&gt;</emphasis>
</simpara>
</listitem>
<listitem>
<simpara>
<emphasis>git://&lt;host&gt;[:&lt;port&gt;]/~&lt;user&gt;/&lt;path-to-git-repo&gt;</emphasis>
</simpara>
</listitem>
<listitem>
<simpara>
<emphasis>[&lt;user&gt;@]&lt;host&gt;:~&lt;user&gt;/&lt;path-to-git-repo&gt;</emphasis>
</simpara>
</listitem>
</itemizedlist>
<simpara>For local repositories, also supported by Git natively, the following
syntaxes may be used:</simpara>
<itemizedlist>
<listitem>
<simpara>
<emphasis>/path/to/repo.git/</emphasis>
</simpara>
</listitem>
<listitem>
<simpara>
<emphasis>file:///path/to/repo.git/</emphasis>
</simpara>
</listitem>
</itemizedlist>
<simpara>These two syntaxes are mostly equivalent, except when cloning, when
the former implies <emphasis>--local</emphasis> option. See <xref linkend="git-clone(1)" /> for
details.</simpara>
<simpara><emphasis>git clone</emphasis>, <emphasis>git fetch</emphasis> and <emphasis>git pull</emphasis>, but not <emphasis>git push</emphasis>, will also
accept a suitable bundle file. See <xref linkend="git-bundle(1)" />.</simpara>
<simpara>When Git doesn't know how to handle a certain transport protocol, it
attempts to use the <emphasis>remote-&lt;transport&gt;</emphasis> remote helper, if one
exists. To explicitly request a remote helper, the following syntax
may be used:</simpara>
<itemizedlist>
<listitem>
<simpara>
<emphasis>&lt;transport&gt;::&lt;address&gt;</emphasis>
</simpara>
</listitem>
</itemizedlist>
<simpara>where <emphasis>&lt;address&gt;</emphasis> may be a path, a server and path, or an arbitrary
URL-like string recognized by the specific remote helper being
invoked. See <xref linkend="gitremote-helpers(7)" /> for details.</simpara>
<simpara>If there are a large number of similarly-named remote repositories and
you want to use a different format for them (such that the URLs you
use will be rewritten into URLs that work), you can create a
configuration section of the form:</simpara>
<blockquote>
<literallayout>        [url "<emphasis>&lt;actual-url-base&gt;</emphasis>"]
                insteadOf = <emphasis>&lt;other-url-base&gt;</emphasis></literallayout>
</blockquote>
<simpara>For example, with this:</simpara>
<screen>        [url "git://git.host.xz/"]
                insteadOf = host.xz:/path/to/
                insteadOf = work:</screen>
<simpara>a URL like "work:repo.git" or like "host.xz:/path/to/repo.git" will be
rewritten in any context that takes a URL to be "git://git.host.xz/repo.git".</simpara>
<simpara>If you want to rewrite URLs for push only, you can create a
configuration section of the form:</simpara>
<blockquote>
<literallayout>        [url "<emphasis>&lt;actual-url-base&gt;</emphasis>"]
                pushInsteadOf = <emphasis>&lt;other-url-base&gt;</emphasis></literallayout>
</blockquote>
<simpara>For example, with this:</simpara>
<screen>        [url "ssh://example.org/"]
                pushInsteadOf = git://example.org/</screen>
<simpara>a URL like "git://example.org/path/to/repo.git" will be rewritten to
"ssh://example.org/path/to/repo.git" for pushes, but pulls will still
use the original URL.</simpara>
</simplesect>
<simplesect id="git-push(1)__remotes_anchor_id_git_push_1__remotes_xreflabel_remotes">
<title>REMOTES<anchor id="git-push(1)_REMOTES" xreflabel="[REMOTES]"/></title>
<simpara>The name of one of the following can be used instead
of a URL as <emphasis>&lt;repository&gt;</emphasis> argument:</simpara>
<itemizedlist>
<listitem>
<simpara>
a remote in the Git configuration file: <emphasis>$GIT_DIR/config</emphasis>,
</simpara>
</listitem>
<listitem>
<simpara>
a file in the <emphasis>$GIT_DIR/remotes</emphasis> directory, or
</simpara>
</listitem>
<listitem>
<simpara>
a file in the <emphasis>$GIT_DIR/branches</emphasis> directory.
</simpara>
</listitem>
</itemizedlist>
<simpara>All of these also allow you to omit the refspec from the command line
because they each contain a refspec which git will use by default.</simpara>
<section id="git-push(1)__named_remote_in_configuration_file">
<title>Named remote in configuration file</title>
<simpara>You can choose to provide the name of a remote which you had previously
configured using <xref linkend="git-remote(1)" />, <xref linkend="git-config(1)" />
or even by a manual edit to the <emphasis>$GIT_DIR/config</emphasis> file.  The URL of
this remote will be used to access the repository.  The refspec
of this remote will be used by default when you do
not provide a refspec on the command line.  The entry in the
config file would appear like this:</simpara>
<screen>        [remote "&lt;name&gt;"]
                url = &lt;URL&gt;
                pushurl = &lt;pushurl&gt;
                push = &lt;refspec&gt;
                fetch = &lt;refspec&gt;</screen>
<simpara>The <emphasis>&lt;pushurl&gt;</emphasis> is used for pushes only. It is optional and defaults
to <emphasis>&lt;URL&gt;</emphasis>. Pushing to a remote affects all defined pushurls or all
defined urls if no pushurls are defined. Fetch, however, will only
fetch from the first defined url if multiple urls are defined.</simpara>
</section>
<section id="git-push(1)__named_file_in_emphasis_git_dir_remotes_emphasis">
<title>Named file in <emphasis>$GIT_DIR/remotes</emphasis></title>
<simpara>You can choose to provide the name of a
file in <emphasis>$GIT_DIR/remotes</emphasis>.  The URL
in this file will be used to access the repository.  The refspec
in this file will be used as default when you do not
provide a refspec on the command line.  This file should have the
following format:</simpara>
<screen>        URL: one of the above URL formats
        Push: &lt;refspec&gt;
        Pull: &lt;refspec&gt;</screen>
<simpara><emphasis>Push:</emphasis> lines are used by <emphasis>git push</emphasis> and
<emphasis>Pull:</emphasis> lines are used by <emphasis>git pull</emphasis> and <emphasis>git fetch</emphasis>.
Multiple <emphasis>Push:</emphasis> and <emphasis>Pull:</emphasis> lines may
be specified for additional branch mappings.</simpara>
</section>
<section id="git-push(1)__named_file_in_emphasis_git_dir_branches_emphasis">
<title>Named file in <emphasis>$GIT_DIR/branches</emphasis></title>
<simpara>You can choose to provide the name of a
file in <emphasis>$GIT_DIR/branches</emphasis>.
The URL in this file will be used to access the repository.
This file should have the following format:</simpara>
<screen>        &lt;URL&gt;#&lt;head&gt;</screen>
<simpara><emphasis>&lt;URL&gt;</emphasis> is required; <emphasis>#&lt;head&gt;</emphasis> is optional.</simpara>
<simpara>Depending on the operation, git will use one of the following
refspecs, if you don't provide one on the command line.
<emphasis>&lt;branch&gt;</emphasis> is the name of this file in <emphasis>$GIT_DIR/branches</emphasis> and
<emphasis>&lt;head&gt;</emphasis> defaults to <emphasis>master</emphasis>.</simpara>
<simpara>git fetch uses:</simpara>
<screen>        refs/heads/&lt;head&gt;:refs/heads/&lt;branch&gt;</screen>
<simpara>git push uses:</simpara>
<screen>        HEAD:refs/heads/&lt;head&gt;</screen>
</section>
</simplesect>
<simplesect id="git-push(1)_UPSTREAM-BRANCHES">
<title>UPSTREAM BRANCHES</title>
<simpara>Branches in Git can optionally have an upstream remote branch.
Git defaults to using the upstream branch for remote operations, for example:</simpara>
<itemizedlist>
<listitem>
<simpara>
It's the default for <emphasis>git pull</emphasis> or <emphasis>git fetch</emphasis> with no arguments.
</simpara>
</listitem>
<listitem>
<simpara>
It's the default for <emphasis>git push</emphasis> with no arguments, with some exceptions.
  For example, you can use the <emphasis>branch.&lt;name&gt;.pushRemote</emphasis> option to push
  to a different remote than you pull from, and by default with
  <emphasis>push.default=simple</emphasis> the upstream branch you configure must have
  the same name.
</simpara>
</listitem>
<listitem>
<simpara>
Various commands, including <emphasis>git checkout</emphasis> and <emphasis>git status</emphasis>, will
  show you how many commits have been added to your current branch and
  the upstream since you forked from it, for example "Your branch and
  <emphasis>origin/main</emphasis> have diverged, and have 2 and 3 different commits each
  respectively".
</simpara>
</listitem>
</itemizedlist>
<simpara>The upstream is stored in <emphasis>.git/config</emphasis>, in the "<emphasis>remote</emphasis>" and "<emphasis>merge</emphasis>"
fields. For example, if <emphasis>main</emphasis>'s upstream is <emphasis>origin/main</emphasis>:</simpara>
<screen>[branch "main"]
   remote = origin
   merge = refs/heads/main</screen>
<simpara>You can set an upstream branch explicitly with
<emphasis>git push --set-upstream &lt;remote&gt; &lt;branch&gt;</emphasis>
but Git will often automatically set the upstream for you, for example:</simpara>
<itemizedlist>
<listitem>
<simpara>
When you clone a repository, Git will automatically set the upstream
  for the default branch.
</simpara>
</listitem>
<listitem>
<simpara>
If you have the <emphasis>push.autoSetupRemote</emphasis> configuration option set,
  <emphasis>git push</emphasis> will automatically set the upstream the first time you push
  a branch.
</simpara>
</listitem>
<listitem>
<simpara>
Checking out a remote-tracking branch with <emphasis>git checkout &lt;branch&gt;</emphasis>
  will automatically create a local branch with that name and set
  the upstream to the remote branch.
</simpara>
</listitem>
</itemizedlist>
<note><simpara>Upstream branches are sometimes referred to as "tracking information",
as in "set the branch's tracking information".</simpara></note>
</simplesect>
<simplesect id="git-push(1)__output">
<title>OUTPUT</title>
<simpara>The output of "git push" depends on the transport method used; this
section describes the output when pushing over the Git protocol (either
locally or via ssh).</simpara>
<simpara>The status of the push is output in tabular form, with each line
representing the status of a single ref. Each line is of the form:</simpara>
<screen> &lt;flag&gt; &lt;summary&gt; &lt;from&gt; -&gt; &lt;to&gt; (&lt;reason&gt;)</screen>
<simpara>If <emphasis>--porcelain</emphasis> is used, then each line of the output is of the form:</simpara>
<screen> &lt;flag&gt; \t &lt;from&gt;:&lt;to&gt; \t &lt;summary&gt; (&lt;reason&gt;)</screen>
<simpara>The status of up-to-date refs is shown only if <emphasis>--porcelain</emphasis> or <emphasis>--verbose</emphasis>
option is used.</simpara>
<variablelist>
<varlistentry>
<term>
<emphasis>&lt;flag&gt;</emphasis>
</term>
<listitem>
<simpara>
        A single character indicating the status of the ref:
</simpara>
<variablelist>
<varlistentry>
<term>
(space)
</term>
<listitem>
<simpara>
for a successfully pushed fast-forward;
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>+</emphasis>
</term>
<listitem>
<simpara>
for a successful forced update;
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>-</emphasis>
</term>
<listitem>
<simpara>
for a successfully deleted ref;
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>*</emphasis>
</term>
<listitem>
<simpara>
for a successfully pushed new ref;
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>!</emphasis>
</term>
<listitem>
<simpara>
for a ref that was rejected or failed to push; and
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>=</emphasis>
</term>
<listitem>
<simpara>
for a ref that was up to date and did not need pushing.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>&lt;summary&gt;</emphasis>
</term>
<listitem>
<simpara>
        For a successfully pushed ref, the summary shows the old and new
        values of the ref in a form suitable for using as an argument to
        <emphasis>git log</emphasis> (this is <emphasis>&lt;old&gt;..&lt;new&gt;</emphasis> in most cases, and
        <emphasis>&lt;old&gt;...&lt;new&gt;</emphasis> for forced non-fast-forward updates).
</simpara>
<simpara>For a failed update, more details are given:</simpara>
<variablelist>
<varlistentry>
<term>
rejected
</term>
<listitem>
<simpara>
        Git did not try to send the ref at all, typically because it
        is not a fast-forward and you did not force the update.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
remote rejected
</term>
<listitem>
<simpara>
        The remote end refused the update.  Usually caused by a hook
        on the remote side, or because the remote repository has one
        of the following safety options in effect:
        <emphasis>receive.denyCurrentBranch</emphasis> (for pushes to the checked out
        branch), <emphasis>receive.denyNonFastForwards</emphasis> (for forced
        non-fast-forward updates), <emphasis>receive.denyDeletes</emphasis> or
        <emphasis>receive.denyDeleteCurrent</emphasis>.  See <xref linkend="git-config(1)" />.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
remote failure
</term>
<listitem>
<simpara>
        The remote end did not report the successful update of the ref,
        perhaps because of a temporary error on the remote side, a
        break in the network connection, or other transient error.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</listitem>
</varlistentry>
<varlistentry>
<term>
from
</term>
<listitem>
<simpara>
        The name of the local ref being pushed, minus its
        <emphasis>refs/&lt;type&gt;/</emphasis> prefix. In the case of deletion, the
        name of the local ref is omitted.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
to
</term>
<listitem>
<simpara>
        The name of the remote ref being updated, minus its
        <emphasis>refs/&lt;type&gt;/</emphasis> prefix.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
reason
</term>
<listitem>
<simpara>
        A human-readable explanation. In the case of successfully pushed
        refs, no explanation is needed. For a failed ref, the reason for
        failure is described.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</simplesect>
<simplesect id="git-push(1)__push_rules">
<title>PUSH RULES</title>
<simpara>As a safety feature, the <emphasis>git push</emphasis> command only allows certain kinds of
updates to prevent you from accidentally losing data on the remote.</simpara>
<simpara>Because branches and tags are intended to be used differently, the
safety rules for pushing to a branch are different from the rules
for pushing to a tag. In the following rules "update" means any
modifications except deletions and creations. Deletions and creations
are always allowed, except when forbidden by configuration or hooks.</simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>
If the push destination is a <emphasis role="strong">branch</emphasis> (<emphasis>refs/heads/*</emphasis>): only
   fast-forward updates are allowed, which means the destination must be
   an ancestor of the source commit. The source must be a commit.
</simpara>
</listitem>
<listitem>
<simpara>
If the push destination is a <emphasis role="strong">tag</emphasis> (<emphasis>refs/tags/*</emphasis>): all updates will
   be rejected. The source can be any object.
</simpara>
</listitem>
<listitem>
<simpara>
If the push destination is not a branch or tag:
</simpara>
<itemizedlist>
<listitem>
<simpara>
If the source is a tree or blob object, any updates will be rejected
</simpara>
</listitem>
<listitem>
<simpara>
If the source is a tag or commit object, any fast-forward update
     is allowed, even in cases where what's being fast-forwarded is not a
     commit, but a tag object which happens to point to a new commit which
     is a fast-forward of the commit the last tag (or commit) it's
     replacing. Replacing a tag with an entirely different tag is also
     allowed, if it points to the same commit, as well as pushing a peeled
     tag, i.e. pushing the commit that existing tag object points to, or a
     new tag object which an existing commit points to.
</simpara>
</listitem>
</itemizedlist>
</listitem>
</orderedlist>
<simpara>You can override these rules by passing <emphasis>--force</emphasis> or by adding the
optional leading <emphasis>+</emphasis> to a refspec. The only exceptions are that no
amount of forcing will make a branch accept a non-commit object,
and forcing won't make the remote repository accept a push that it's
configured to deny.</simpara>
<simpara>Hooks and configuration can also override or amend these rules,
see e.g. <emphasis>receive.denyNonFastForwards</emphasis> and <emphasis>receive.denyDeletes</emphasis>
in <xref linkend="git-config(1)" /> and <emphasis>pre-receive</emphasis> and <emphasis>update</emphasis> in
<xref linkend="githooks(5)" />.</simpara>
</simplesect>
<simplesect id="git-push(1)__note_about_fast_forwards">
<title>NOTE ABOUT FAST-FORWARDS</title>
<simpara>When an update changes a branch (or more in general, a ref) that used to
point at commit A to point at another commit B, it is called a
fast-forward update if and only if B is a descendant of A.</simpara>
<simpara>In a fast-forward update from A to B, the set of commits that the original
commit A built on top of is a subset of the commits the new commit B
builds on top of.  Hence, it does not lose any history.</simpara>
<simpara>In contrast, a non-fast-forward update will lose history.  For example,
suppose you and somebody else started at the same commit X, and you built
a history leading to commit B while the other person built a history
leading to commit A.  The history looks like this:</simpara>
<screen>      B
     /
 ---X---A</screen>
<simpara>Further suppose that the other person already pushed changes leading to A
back to the original repository from which you two obtained the original
commit X.</simpara>
<simpara>The push done by the other person updated the branch that used to point at
commit X to point at commit A.  It is a fast-forward.</simpara>
<simpara>But if you try to push, you will attempt to update the branch (that
now points at A) with commit B.  This does <emphasis>not</emphasis> fast-forward.  If you did
so, the changes introduced by commit A will be lost, because everybody
will now start building on top of B.</simpara>
<simpara>The command by default does not allow an update that is not a fast-forward
to prevent such loss of history.</simpara>
<simpara>If you do not want to lose your work (history from X to B) or the work by
the other person (history from X to A), you would need to first fetch the
history from the repository, create a history that contains changes done
by both parties, and push the result back.</simpara>
<simpara>You can perform "git pull", resolve potential conflicts, and "git push"
the result.  A "git pull" will create a merge commit C between commits A
and B.</simpara>
<screen>      B---C
     /   /
 ---X---A</screen>
<simpara>Updating A with the resulting merge commit will fast-forward and your
push will be accepted.</simpara>
<simpara>Alternatively, you can rebase your change between X and B on top of A,
with <emphasis>git pull --rebase</emphasis>, and push the result back.  The rebase will
create a new commit D that builds the change between X and B on top of
A.</simpara>
<screen>      B   D
     /   /
 ---X---A</screen>
<simpara>Again, updating A with this commit will fast-forward and your push will be
accepted.</simpara>
<simpara>There is another common situation where you may encounter non-fast-forward
rejection when you try to push, and it is possible even when you are
pushing into a repository nobody else pushes into. After you push commit
A yourself (in the first picture in this section), replace it with <emphasis>git
commit --amend</emphasis> to produce commit B, and you try to push it out, because
forgot that you have pushed A out already. In such a case, and only if
you are certain that nobody in the meantime fetched your earlier commit A
(and started building on top of it), you can run <emphasis>git push --force</emphasis> to
overwrite it. In other words, <emphasis>git push --force</emphasis> is a method reserved for
a case where you do mean to lose history.</simpara>
</simplesect>
<simplesect id="git-push(1)__examples">
<title>EXAMPLES</title>
<variablelist>
<varlistentry>
<term>
<emphasis>git push</emphasis>
</term>
<listitem>
<simpara>
        Works like <emphasis>git push &lt;remote&gt;</emphasis>, where &lt;remote&gt; is the
        current branch's remote (or <emphasis>origin</emphasis>, if no remote is
        configured for the current branch).
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>git push origin</emphasis>
</term>
<listitem>
<simpara>
        Without additional configuration, pushes the current branch to
        the configured upstream (<emphasis>branch.&lt;name&gt;.merge</emphasis> configuration
        variable) if it has the same name as the current branch, and
        errors out without pushing otherwise.
</simpara>
<simpara>The default behavior of this command when no <emphasis>&lt;refspec&gt;</emphasis> is given can be
configured by setting the <emphasis>push</emphasis> option of the remote, or the <emphasis>push.default</emphasis>
configuration variable.</simpara>
<simpara>For example, to default to pushing only the current branch to <emphasis>origin</emphasis>
use <emphasis>git config remote.origin.push HEAD</emphasis>.  Any valid <emphasis>&lt;refspec&gt;</emphasis> (like
the ones in the examples below) can be configured as the default for
<emphasis>git push origin</emphasis>.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>git push origin :</emphasis>
</term>
<listitem>
<simpara>
        Push "matching" branches to <emphasis>origin</emphasis>. See
        <emphasis>&lt;refspec&gt;</emphasis> in the <link linkend="git-push(1)_OPTIONS">OPTIONS</link> section above for a
        description of "matching" branches.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>git push origin master</emphasis>
</term>
<listitem>
<simpara>
        Find a ref that matches <emphasis>master</emphasis> in the source repository
        (most likely, it would find <emphasis>refs/heads/master</emphasis>), and update
        the same ref (e.g. <emphasis>refs/heads/master</emphasis>) in <emphasis>origin</emphasis> repository
        with it.  If <emphasis>master</emphasis> did not exist remotely, it would be
        created.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>git push origin HEAD</emphasis>
</term>
<listitem>
<simpara>
        A handy way to push the current branch to the same name on the
        remote.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>git push mothership master:satellite/master dev:satellite/dev</emphasis>
</term>
<listitem>
<simpara>
        Use the source ref that matches <emphasis>master</emphasis> (e.g. <emphasis>refs/heads/master</emphasis>)
        to update the ref that matches <emphasis>satellite/master</emphasis> (most probably
        <emphasis>refs/remotes/satellite/master</emphasis>) in the <emphasis>mothership</emphasis> repository;
        do the same for <emphasis>dev</emphasis> and <emphasis>satellite/dev</emphasis>.
</simpara>
<simpara>See the section describing <emphasis>&lt;refspec&gt;...</emphasis> above for a discussion of
the matching semantics.</simpara>
<simpara>This is to emulate <emphasis>git fetch</emphasis> run on the <emphasis>mothership</emphasis> using <emphasis>git
push</emphasis> that is run in the opposite direction in order to integrate
the work done on <emphasis>satellite</emphasis>, and is often necessary when you can
only make connection in one way (i.e. satellite can ssh into
mothership but mothership cannot initiate connection to satellite
because the latter is behind a firewall or does not run sshd).</simpara>
<simpara>After running this <emphasis>git push</emphasis> on the <emphasis>satellite</emphasis> machine, you would
ssh into the <emphasis>mothership</emphasis> and run <emphasis>git merge</emphasis> there to complete the
emulation of <emphasis>git pull</emphasis> that were run on <emphasis>mothership</emphasis> to pull changes
made on <emphasis>satellite</emphasis>.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>git push origin HEAD:master</emphasis>
</term>
<listitem>
<simpara>
        Push the current branch to the remote ref matching <emphasis>master</emphasis> in the
        <emphasis>origin</emphasis> repository. This form is convenient to push the current
        branch without thinking about its local name.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>git push origin master:refs/heads/experimental</emphasis>
</term>
<listitem>
<simpara>
        Create the branch <emphasis>experimental</emphasis> in the <emphasis>origin</emphasis> repository
        by copying the current <emphasis>master</emphasis> branch.  This form is only
        needed to create a new branch or tag in the remote repository when
        the local name and the remote name are different; otherwise,
        the ref name on its own will work.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>git push origin :experimental</emphasis>
</term>
<listitem>
<simpara>
        Find a ref that matches <emphasis>experimental</emphasis> in the <emphasis>origin</emphasis> repository
        (e.g. <emphasis>refs/heads/experimental</emphasis>), and delete it.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>git push origin +dev:master</emphasis>
</term>
<listitem>
<simpara>
        Update the origin repository's master branch with the dev branch,
        allowing non-fast-forward updates.  <emphasis role="strong">This can leave unreferenced
        commits dangling in the origin repository.</emphasis>  Consider the
        following situation, where a fast-forward is not possible:
</simpara>
<screen>            o---o---o---A---B  origin/master
                     \
                      X---Y---Z  dev</screen>
<simpara>The above command would change the origin repository to</simpara>
<screen>                      A---B  (unnamed branch)
                     /
            o---o---o---X---Y---Z  master</screen>
<simpara>Commits A and B would no longer belong to a branch with a symbolic name,
and so would be unreachable.  As such, these commits would be removed by
a <emphasis>git gc</emphasis> command on the origin repository.</simpara>
</listitem>
</varlistentry>
</variablelist>
</simplesect>
<simplesect id="git-push(1)__security">
<title>SECURITY</title>
<simpara>The fetch and push protocols are not designed to prevent one side from
stealing data from the other repository that was not intended to be
shared. If you have private data that you need to protect from a malicious
peer, your best option is to store it in another repository. This applies
to both clients and servers. In particular, namespaces on a server are not
effective for read access control; you should only grant read access to a
namespace to clients that you would trust with read access to the entire
repository.</simpara>
<simpara>The known attack vectors are as follows:</simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>
The victim sends "have" lines advertising the IDs of objects it has that
  are not explicitly intended to be shared but can be used to optimize the
  transfer if the peer also has them. The attacker chooses an object ID X
  to steal and sends a ref to X, but isn't required to send the content of
  X because the victim already has it. Now the victim believes that the
  attacker has X, and it sends the content of X back to the attacker
  later. (This attack is most straightforward for a client to perform on a
  server, by creating a ref to X in the namespace the client has access
  to and then fetching it. The most likely way for a server to perform it
  on a client is to "merge" X into a public branch and hope that the user
  does additional work on this branch and pushes it back to the server
  without noticing the merge.)
</simpara>
</listitem>
<listitem>
<simpara>
As in #1, the attacker chooses an object ID X to steal. The victim sends
  an object Y that the attacker already has, and the attacker falsely
  claims to have X and not Y, so the victim sends Y as a delta against X.
  The delta reveals regions of X that are similar to Y to the attacker.
</simpara>
</listitem>
</orderedlist>
</simplesect>
<simplesect id="git-push(1)_CONFIGURATION">
<title>CONFIGURATION</title>
<simpara>Everything below this line in this section is selectively included
from the <xref linkend="git-config(1)" /> documentation. The content is the same
as what's found there:</simpara>
<variablelist>
<varlistentry>
<term>
<emphasis>push.autoSetupRemote</emphasis>
</term>
<listitem>
<simpara>
        If set to <emphasis>true</emphasis> assume <emphasis>--set-upstream</emphasis> on default push when no
        upstream tracking exists for the current branch; this option
        takes effect with <emphasis>push.default</emphasis> options <emphasis>simple</emphasis>, <emphasis>upstream</emphasis>,
        and <emphasis>current</emphasis>. It is useful if by default you want new branches
        to be pushed to the default remote (like the behavior of
        <emphasis>push.default=current</emphasis>) and you also want the upstream tracking
        to be set. Workflows most likely to benefit from this option are
        <emphasis>simple</emphasis> central workflows where all branches are expected to
        have the same name on the remote.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>push.default</emphasis>
</term>
<listitem>
<simpara>
        Defines the action <emphasis>git push</emphasis> should take if no refspec is
        given (whether from the command-line, config, or elsewhere).
        Different values are well-suited for
        specific workflows; for instance, in a purely central workflow
        (i.e. the fetch source is equal to the push destination),
        <emphasis>upstream</emphasis> is probably what you want.  Possible values are:
</simpara>
<variablelist>
<varlistentry>
<term>
<emphasis>nothing</emphasis>
</term>
<listitem>
<simpara>
do not push anything (error out) unless a refspec is
given. This is primarily meant for people who want to
avoid mistakes by always being explicit.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>current</emphasis>
</term>
<listitem>
<simpara>
push the current branch to update a branch with the same
name on the receiving end.  Works in both central and non-central
workflows.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>upstream</emphasis>
</term>
<listitem>
<simpara>
push the current branch back to the branch whose
changes are usually integrated into the current branch (which is
called <emphasis>@{upstream}</emphasis>).  This mode only makes sense if you are
pushing to the same repository you would normally pull from
(i.e. central workflow).
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>tracking</emphasis>
</term>
<listitem>
<simpara>
this is a deprecated synonym for <emphasis>upstream</emphasis>.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>simple</emphasis>
</term>
<listitem>
<simpara>
push the current branch with the same name on the remote.
</simpara>
<simpara>If you are working on a centralized workflow (pushing to the same repository you
pull from, which is typically <emphasis>origin</emphasis>), then you need to configure an upstream
branch with the same name.</simpara>
<simpara>This mode is the default since Git 2.0, and is the safest option suited for
beginners.</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>matching</emphasis>
</term>
<listitem>
<simpara>
push all branches having the same name on both ends.
This makes the repository you are pushing to remember the set of
branches that will be pushed out (e.g. if you always push <emphasis>maint</emphasis>
and <emphasis>master</emphasis> there and no other branches, the repository you push
to will have these two branches, and your local <emphasis>maint</emphasis> and
<emphasis>master</emphasis> will be pushed there).
</simpara>
<simpara>To use this mode effectively, you have to make sure <emphasis>all</emphasis> the
branches you would push out are ready to be pushed out before
running <emphasis>git push</emphasis>, as the whole point of this mode is to allow you
to push all of the branches in one go.  If you usually finish work
on only one branch and push out the result, while other branches are
unfinished, this mode is not for you.  Also this mode is not
suitable for pushing into a shared central repository, as other
people may add new branches there, or update the tip of existing
branches outside your control.</simpara>
<simpara>This used to be the default, but not since Git 2.0 (<emphasis>simple</emphasis> is the
new default).</simpara>
</listitem>
</varlistentry>
</variablelist>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>push.followTags</emphasis>
</term>
<listitem>
<simpara>
        If set to true, enable <emphasis>--follow-tags</emphasis> option by default.  You
        may override this configuration at time of push by specifying
        <emphasis>--no-follow-tags</emphasis>.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>push.gpgSign</emphasis>
</term>
<listitem>
<simpara>
        May be set to a boolean value, or the string <emphasis>if-asked</emphasis>. A true
        value causes all pushes to be GPG signed, as if <emphasis>--signed</emphasis> is
        passed to <xref linkend="git-push(1)" />. The string <emphasis>if-asked</emphasis> causes
        pushes to be signed if the server supports it, as if
        <emphasis>--signed=if-asked</emphasis> is passed to <emphasis>git push</emphasis>. A false value may
        override a value from a lower-priority config file. An explicit
        command-line flag always overrides this config option.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>push.pushOption</emphasis>
</term>
<listitem>
<simpara>
        When no <emphasis>--push-option=&lt;option&gt;</emphasis> argument is given from the
        command line, <emphasis>git push</emphasis> behaves as if each <emphasis>&lt;option&gt;</emphasis> of
        this variable is given as <emphasis>--push-option=&lt;option&gt;</emphasis>.
</simpara>
<simpara>This is a multi-valued variable, and an empty value can be used in a
higher priority configuration file (e.g. <emphasis>.git/config</emphasis> in a
repository) to clear the values inherited from a lower priority
configuration files (e.g. <emphasis>$HOME/.gitconfig</emphasis>).</simpara>
<screen>Example:

/etc/gitconfig
  push.pushoption = a
  push.pushoption = b

~/.gitconfig
  push.pushoption = c

repo/.git/config
  push.pushoption =
  push.pushoption = b

This will result in only b (a and c are cleared).</screen>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>push.recurseSubmodules</emphasis>
</term>
<listitem>
<simpara>
        May be <emphasis>check</emphasis>, <emphasis>on-demand</emphasis>, <emphasis>only</emphasis>, or <emphasis>no</emphasis>, with the same behavior
        as that of <emphasis>push --recurse-submodules</emphasis>.
        If not set, <emphasis>no</emphasis> is used by default, unless <emphasis>submodule.recurse</emphasis> is
        set (in which case a <emphasis>true</emphasis> value means <emphasis>on-demand</emphasis>).
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>push.useForceIfIncludes</emphasis>
</term>
<listitem>
<simpara>
        If set to <emphasis>true</emphasis>, it is equivalent to specifying
        <emphasis>--force-if-includes</emphasis> as an option to <xref linkend="git-push(1)" />
        in the command line. Adding <emphasis>--no-force-if-includes</emphasis> at the
        time of push overrides this configuration setting.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>push.negotiate</emphasis>
</term>
<listitem>
<simpara>
        If set to <emphasis>true</emphasis>, attempt to reduce the size of the packfile
        sent by rounds of negotiation in which the client and the
        server attempt to find commits in common. If <emphasis>false</emphasis>, Git will
        rely solely on the server's ref advertisement to find commits
        in common.
</simpara>
</listitem>
</varlistentry>
<varlistentry>
<term>
<emphasis>push.useBitmaps</emphasis>
</term>
<listitem>
<simpara>
        If set to <emphasis>false</emphasis>, disable use of bitmaps for <emphasis>git push</emphasis> even if
        <emphasis>pack.useBitmaps</emphasis> is <emphasis>true</emphasis>, without preventing other git operations
        from using bitmaps. Default is <emphasis>true</emphasis>.
</simpara>
</listitem>
</varlistentry>
</variablelist>
</simplesect>
<simplesect id="git-push(1)__git">
<title>GIT</title>
<simpara>Part of the <xref linkend="git(1)" /> suite</simpara>
</simplesect>
</sect2>
