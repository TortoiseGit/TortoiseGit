<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE sect1 SYSTEM "../../../dtd/dblite.dtd">
<sect1 id="tgit-dug-pull">
  <title>Pull and Fetch change</title>
  <?dbhh topicname="HIDD_PULLFETCH"?>
  <indexterm>
    <primary>Pull</primary>
  </indexterm>
  <indexterm>
    <primary>Fetch</primary>
  </indexterm>
  <para>
    This section talks about how to fetch or pull change from the other
	repository. The difference between pull and fetch is:
  </para>
  <para>
	Pull will merge remote branch change to local branch which tracked with 
	remote branch. Fetch just get change from remote branch and never merge.
  </para>
  <para>
    <menuchoice>
      <guimenu>TortoiseGit
      </guimenu>
	  <guimenuitem>Pull...
	  </guimenuitem>
    </menuchoice>. 
  </para>
  <para>
    <menuchoice>
      <guimenu>TortoiseGit
      </guimenu>
	  <guimenuitem>Fetch...
	  </guimenuitem>
    </menuchoice>. 
  </para>
  <para>
    Pull\fetch Dialog will show. <graphic fileref="../images/GitPull.png"/> <graphic fileref="../images/Fetch.png"/>
  </para>
  <para>
    <guilabel>Remote:</guilabel> Choose Remote
  </para>
  <para>
    <guilabel>Other:</guilabel> Input Other URL or local directory.
    You can click <guibutton>...</guibutton> to browse directory. 
  </para>
  <para>
    If you check the <guilabel>Autoload Putty Key</guilabel> checkbox, Putty key
	will auto load by Pageant.
  </para>
  <para>
    <guilabel>Manage Remotes</guilabel> to add or modify remote configuration.
  </para>
  <para>
    <guilabel>Tags</guilabel> has three states (git 1.9 and later): Checked: All tags as well as branches are downloaded (--tags is passed to git), unchecked: No tags are downloaded (--no-tags is passed to git), and third state: use default behavior (based on remote <option>name</option>.tagopt setting).
    <guilabel>Tags</guilabel> has three states (prior to git 1.9): Only all tags are downloaded but no branches are downloaded (--tags is passed to git), unchecked: No tags are downloaded (--no-tags is passed to git), and third state: use default behavior (based on remote <option>name</option>.tagopt setting).
  </para>
  <para>
    <guilabel>Prune</guilabel> has three states: True to remove remote-tracking branches which no longer exist on the remote, false: not to remove, and third state: use default behavior (based on remote <option>name</option>.prune or fetch.prune setting).
  </para>
  <para>
    <guilabel>Depth</guilabel> allows you to do a shallow pull/fetch on a shallow-clone repository with a specified depth.
  </para>
  <tip>
    <para>You can find more information about PuTTY and using ssh-keys at <xref linkend="tgit-ssh-howto"/>.</para>
  </tip>
  <para>
    You can find more information at <xref linkend="git-fetch(1)"/> and <xref linkend="git-pull(1)"/>.
  </para>
</sect1>
